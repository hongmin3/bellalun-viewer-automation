# Progress Checkpoint

## 2026-09-01 XIPL_05 불합격 경계 검증 완료 — 다음은 전체 회귀 1회

- 이전 체크포인트(WF_06 RDSR 오염 수정, 커밋 `42cc1b2`)에 이어 `XIPL_05` "불합격
  경계값 입력 시 Fail"이 MANUAL로 남던 문제를 조사·수정했다.
- 원인: Fiber 콤보에 합격 기준(4.0) 미만 항목이 "없다"고 봤던 08-28 결론이 오판이었다.
  실제로는 14개 항목(6.0~0.0, 0.5 간격)이 있는데 팝업(`ItemWnd`)이 6~7개만 보여주고
  나머지는 스크롤해야 나온다 — `IsWindowVisible`은 참이라 안 보이는 항목도 "있다"고
  잡히지만 OCR은 깨진 문구를 읽는다. `tests/xipl_flows.py::_qc_pick_score`가 스크롤을
  안 해서 못 찾은 것이었다(`Temp/probe_qc_fiber.py`로 조회 전용 라이브 조사 후 확정).
- 수정: `_qc_pick_score`가 첫 화면에서 후보를 못 찾으면 `_pick_combo_item`과 같은
  방식으로 팝업을 한 칸씩 스크롤하며 재시도하도록 변경. 이미 동작하던 합격 경계
  케이스(스크롤 불필요)는 그대로 — 회귀 없음.
- 검증: `py_compile` 통과, 정적 검사 4종 통과, 단위시험 150건 통과, `run-xipl-05`
  라이브 전 Step PASS(Fiber=3.5 입력 → `QC_STUDY.Result=0` 확인, MANUAL 0건).
- 변경 파일: `tests/xipl_flows.py`(`_qc_pick_score`), `NEXT_WORK.md`(2절 XIPL_05 행,
  3절 #1, 4절 #11, 6절 P0 갱신), `progress.md`. (`Temp/probe_qc_fiber.py`는 조사용
  1회성 스크립트로 커밋 대상 아님.)
- 다음 작업: 개별 TC 재검증은 이걸로 끝. **전체 회귀 1회**만 남았다 — 시작 전 사용자에게
  적용 변경·예상 소요 시간·판정 동일성 기대·남은 위험을 보고하고 진행할 것
  (`NEXT_WORK.md` 6절 P0 #3).

> 장시간/대규모 작업 후 현재 상태만 갱신한다. 완료 이력을 누적하지 않는다.

- 진행 중 작업: 없음.
- 알려진 문제: `NEXT_WORK.md` 3절 참고(제품 결함 3-A, 간헐 증상 3-B/3-C — 전부
  자동화 결함 아님, 회귀 없음).
